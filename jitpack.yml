jdk:
  - openjdk17

before_install:
  - ./gradlew --version

# 跳过 app 模块的构建，只构建库模块
install:
  - ./gradlew clean :selector:assembleRelease :selector:publishToMavenLocal -PisJitpack=true -x test -x lint

